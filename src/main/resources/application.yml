spring:
  datasource:
    url: jdbc:postgresql://ep-blue-mountain-ad5nhyod-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_cpViMu5TF2Jx
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate   # Si querés que Spring cree tablas automáticamente podés poner "update"
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

# Opcional: aumenta el tiempo de espera y conexiones si tu DB lo requiere
# spring:
#   datasource:
#     hikari:
#       maximum-pool-size: 10
#       connection-timeout: 30000
#       idle-timeout: 600000
#       max-lifetime: 1800000